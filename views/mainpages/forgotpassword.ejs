<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Forgot Password</title>
    <link href="/styles/styles.css" rel="stylesheet" />
  </head>
  <body>
    <div class="container-login-main min-vh-100">
      <div class="section-main rounded-5 login-form text-light">
        <div class="logo-brandname mx-5">
          <img class="logo-img" src="/images/logo.jpg" alt="Logo" />
        </div>

        <div class="user-credentials-form submit-form">
          <% if (status) { %>
            <div class="alert alert-info">
              <%= status %>
            </div>

            <% if (status === 'PASSWORD CHANGED SUCCESSFULLY') { %>
              <div class="login-redirect">
                <a href="/" class="text-light"><p>Click to log in with your new password.</p></a>
              </div>
            <% } %>
          <% } %>

          <form action="/passwordreset" method="post">
            <label for="useremail" class="form-label fw-semibold">User Email</label>
            <input
              type="text"
              name="useremail"
              id="useremail"
              class="form-control"
              placeholder='"abc@xyz.com"'
              required
            />

            <label for="newpassword" class="form-label fw-semibold">Password</label>
            <input
              type="password"
              name="newpassword"
              id="newpassword"
              class="form-control"
              required
              minlength="8"
              placeholder="new password"
            />

            <input
              type="submit"
              class="login-button btn btn-secondary float-end mt-3"
              name="login"
              id="submit"
              value="Reset Password"
            />
            <a class="mt-3 clearfix no-decor" href="/"
            >You got it? Log in</a
          >
          </form>
        </div>
      </div>
    </div>

    <footer class="footer bg-dark text-light">
      <div class="copyright">
        <p>&copy;NagaSaiKrishna2024</p>
      </div>
      <div class="footer-help">
        <a href="mailto:nskbobby@gmail.com" class="no-decor" alt="help email">Help</a>
        <a href="mailto:nskbobby@gmail.com" class="no-decor" alt="help email">About</a>
        <a href="mailto:nskbobby@gmail.com" class="no-decor" alt="help email">Services</a>
      </div>
    </footer>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </body>
</html>
